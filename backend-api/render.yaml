services:
  - type: web
    name: stockinventory-backend
    env: docker
    plan: starter
    dockerContext: ./backend-api
    dockerfilePath: ./backend-api/Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      # Set these in Render dashboard (or keep as envVar entries without values)
      # - ADMIN_MONGO_URI
      # - JWT_SECRET
      # - AWS_ACCESS_KEY_ID
      # - AWS_SECRET_ACCESS_KEY
      # - AWS_REGION
      # - AWS_S3_BUCKET_NAME
      # - SES_FROM_EMAIL
      # - TWILIO_ACCOUNT_SID
      # - TWILIO_AUTH_TOKEN
      # - TWILIO_API_KEY_SID
      # - TWILIO_API_KEY_SECRET
      # - TWILIO_WHATSAPP_FROM
      # - CORS_ORIGIN
